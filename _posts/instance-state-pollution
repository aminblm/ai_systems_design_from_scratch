Instance State Pollution (self.client_socket): Storing self.client_socket on self (self.client_socket = ...) is extremely dangerous. If this server were multi-threaded, a new connection would overwrite self.client_socket while a previous thread was running, causing cross-talk where data meant for Client A gets transmitted to Client B. Client sockets should always be local variables passed into handler methods.