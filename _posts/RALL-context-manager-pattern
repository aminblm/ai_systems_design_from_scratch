The RAII Context Manager Pattern (__enter__ / __exit__): Moving socket assembly inside a standard python context block guarantees the underlying socket's close() routine runs even if an explicit runtime error occurs midway through the operation.

